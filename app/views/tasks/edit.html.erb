<h1>タスク編集画面</h1>
<%= form_with model: @task, local: true do |f| %>
    <div>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>
    <div>
        <%= f.label :content %>
        <%= f.text_area :content %>
    </div>
    <%= f.submit "保存" %>
<% end %>